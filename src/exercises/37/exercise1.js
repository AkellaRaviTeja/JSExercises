// Execute this function,
//  - comment the reason for failure.
//  - Fix the issue.


export function printOneToTen() {
  for (let number = 1; number <= 1; number++) {
    console.log(`${number}`);
  }
}

// const cannot be changed once declared So added let instead of that